using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio2 : MonoBehaviour
{
    int edad, minutos, codigoPostal, numeroDeHijos, dias, contador, tallaCamisa;
    float altura, iva, peso, precio;
    string nombre, matriculaCoche, tallaCamisetas, mensaje;
    char letra, genero;
    bool alumnoRepetidor, mayorDeEdad;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
